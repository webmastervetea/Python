Aplicaciones en python

 💻 Automatización y Utilidades
	AYU-1  **Renombrador masivo de archivos:** Script para renombrar múltiples archivos en un directorio.
	AYU-2  **Organizador de archivos:** Mueve archivos a carpetas basadas en su extensión (`.pdf`, `.jpg`, etc.).
	AYU-3  **Descargador de YouTube:** Usa `pytube` para descargar videos o solo audio.
	AYU-4  **Lector de códigos QR:** Genera y lee códigos QR con la librería `qrcode`.
	AYU-5  **Extractor de texto de PDF:** Utiliza `PyPDF2` para obtener texto de documentos PDF.
	AYU-6  **Automatización de Excel:** Script que lee y actualiza datos en una hoja de cálculo (`openpyxl` o `pandas`).
	AYU-7  **Web Scraper simple:** Extrae datos de una página web específica (`requests` y `BeautifulSoup`).
	AYU-8  **Monitor de portapapeles:** Guarda automáticamente el historial de lo que copias.
	AYU-9  **Detector de plagio básico:** Compara dos archivos de texto para encontrar similitudes.
	AYU-10 **Convertidor de unidades:** Convierte, por ejemplo, de Celsius a Fahrenheit.

Soporte: https://www.linkedin.com/in/oscarlizarragag/
