# Python
Este repositorio es una colección organizada de pequeñas aplicaciones, scripts utilitarios y ejemplos funcionales desarrollados en Python.  El objetivo es ofrecer soluciones rápidas, demostrar el uso práctico de librerías específicas y servir como un sandbox de referencia para diferentes tareas de programación.

## 💻 Automatización y Utilidades 
	1.  **Renombrador masivo de archivos:** Script para renombrar múltiples archivos en un directorio.
	2.  **Organizador de archivos:** Mueve archivos a carpetas basadas en su extensión (`.pdf`, `.jpg`, etc.).
	3.  **Descargador de YouTube:** Usa `pytube` para descargar videos o solo audio.
	4.  **Lector de códigos QR:** Genera y lee códigos QR con la librería `qrcode`.
	5.  **Extractor de texto de PDF:** Utiliza `PyPDF2` para obtener texto de documentos PDF.
	6.  **Automatización de Excel:** Script que lee y actualiza datos en una hoja de cálculo (`openpyxl` o `pandas`).
	7.  **Web Scraper simple:** Extrae datos de una página web específica (`requests` y `BeautifulSoup`).
	8.  **Monitor de portapapeles:** Guarda automáticamente el historial de lo que copias.
	9.  **Detector de plagio básico:** Compara dos archivos de texto para encontrar similitudes.
	10. **Convertidor de unidades:** Convierte, por ejemplo, de Celsius a Fahrenheit.
